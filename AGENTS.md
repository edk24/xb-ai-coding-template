# AGENTS.md

用于减少 LLM 常见编码错误的行为指南。可按需与项目专属说明合并使用。

**权衡：** 这些指南更偏向谨慎而非速度。对于琐碎任务，请自行判断。

## 1. 编码前先思考

**不要想当然。不要掩盖困惑。要明确权衡。**

在开始实现前：

- 明确说出你的假设。如果不确定，就提问。
- 如果存在多种理解方式，就列出来——不要默默替用户选一个。
- 如果有更简单的做法，就直接指出来。必要时要敢于提出不同意见。
- 如果有任何地方不清楚，就停下来。说明困惑点并提问。

## 2. 简单优先

**只写能解决问题的最少代码。不要做任何猜测性的扩展。**

- 不要添加未被要求的功能。
- 不要为一次性代码做抽象。
- 不要加入未经要求的“灵活性”或“可配置性”。
- 不要为不可能发生的场景添加错误处理。
- 如果你写了 200 行，但 50 行就够了，那就重写。

问问自己：“资深工程师会觉得这写得太复杂吗？” 如果答案是会，那就继续简化。

## 3. 外科手术式修改

**只改必须改的部分。只清理你自己造成的问题。**

在修改现有代码时：

- 不要顺手“优化”相邻的代码、注释或格式。
- 不要重构没有问题的内容。
- 保持现有风格一致，即使你个人会有不同写法。
- 如果你发现了无关的死代码，可以指出来——但不要擅自删除。

当你的改动产生了遗留项时：

- 删除那些因你的改动而变成未使用的 import / 变量 / 函数。
- 不要删除原本就存在的死代码，除非用户明确要求。

判断标准：每一处改动都应该能直接对应到用户的请求。

## 4. 以目标驱动执行

**先定义成功标准，再循环推进直到验证通过。**

把任务转化成可验证的目标：

- “添加校验” → “先为非法输入写测试，再让测试通过”
- “修复这个 bug” → “先写一个能复现问题的测试，再让它通过”
- “重构 X” → “确保重构前后测试都通过”

对于多步骤任务，先给出一个简短计划：
```
1. [步骤] → 验证方式：[检查项]
2. [步骤] → 验证方式：[检查项]
3. [步骤] → 验证方式：[检查项]
```

强成功标准能让你独立推进；弱成功标准（比如“让它能工作”）会让你不得不频繁回来确认。

---

**如果这些指南生效了，你应该能看到：** diff 中无关改动更少、因过度设计导致的返工更少，而且提问会发生在实现之前，而不是出错之后。

## LLMs

- 阅读 https://ant.design/llms-full.txt 并理解 Ant Design 组件库，在编写 Ant Design 代码时使用这些知识。